# Binly 🚀

**Binly** is a lightweight Linux command manager that allows you to create, edit, and organize executable scripts within `~/.local/bin` through a clean and intuitive Graphical User Interface (GUI).

## ✨ Features
- **One-Click Commands**: Turn any script or application into a global terminal command.
- **Integrated Editor**: Modify your scripts directly within the app.
- **Permission Management**: Easily toggle executable bits (`chmod +x`).
- **Search System**: Quickly filter through your custom commands.

---

## 📥 Installation

### Option 1: Using the Binary (Quickest)
1. Download the `binly-release.zip`.
2. Extract the content.
3. Run the `installer` executable.
4. Follow the GUI instructions to install Binly to your `~/.local/bin`.

### Option 2: Running from Source
If you prefer to run it using Python:
1. Clone this repository:
   ```bash
   git clone [https://github.com/your-username/binly.git](https://github.com/your-username/binly.git)
   cd binly/src
