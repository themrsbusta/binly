# binly
Binly is a lightweight Linux command manager that lets you create, edit, and organize scripts in ~/.local/bin via a clean GUI. It features an integrated editor, permission management (chmod +x), and a search system to turn any script into a global terminal command. Perfect for streamlining your workflow on Linux.
